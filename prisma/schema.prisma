// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"    // Prisma Client generator
  output   = "../generated/prisma" // Prisma Client generatsiya qilgan fayllarni qayerga joylashtirishni belgilaydi
}

datasource db {
  provider = "postgresql"          // DB turi: postgresql, mysql, sqlite, mongo
  url      = env("DATABASE_URL")   // Muhit o‘zgaruvchisi
}
